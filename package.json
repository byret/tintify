{
  "name": "pixapp-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-postbuild": "cd pixapp-frontend && npm install && npm run build"
  },
  "engines": {
    "node": "16.x"
  }
}
