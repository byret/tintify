{
  "name": "pixapp-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm install --prefix pixapp-frontend && npm run build --prefix pixapp-frontend && chmod +x node_modules/.bin/react-scripts"
  },
  "engines": {
    "node": "16.x"
  }
}
